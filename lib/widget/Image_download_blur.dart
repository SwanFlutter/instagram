import 'package:flutter/material.dart';

class ImageDownloadBlur extends StatelessWidget {
  const ImageDownloadBlur({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ImageDownloadBlur'),
      ),
      body: const Center(
        child: Text('ImageDownloadBlur'),
      ),
    );
  }
}
